# SDD ledger — plan: docs/superpowers/plans/2026-09-16-ha-binding-b.md
Spec: docs/superpowers/specs/2026-09-15-cover-automation-design.md (rev 3.3). Feature worktree .worktrees/ha-binding-b, branch feature/ha-binding-b from main 86d3c055f78b3b5cb398a72412ed3b234ddf3ae1. Root .venv symlinked into each worktree.
Execution shape (user's standing instruction: parallelize independent tasks in worktrees; reconcile crossovers before merge):
  wave 1: task-b0 = Task 0 (2a backlog sweep) ‖ task-b1 = Task 1 (units, forecast) — disjoint files
  wave 2: Task 2 (repairs/views/entity + __init__ sweep) ‖ Task 3 (signals_adapter) ‖ Task 4 (scheduler)
  wave 3: Task 5 (controller) ‖ Task 6 (switch/select/button) ‖ Task 7 (sensor/binary_sensor) — 6/7 share tests/ha/test_entities.py (append-only, reconcile)
  wave 4: Task 8 (services/logbook/diagnostics); wave 5: Task 9 (wiring, translations, README, scenarios)
  crossovers: en.json (T0 data_description/errors, T2 issues, T9 entity/services — JSON union, recursive sort); __init__.py (T0 → T2 → T9 sequential by wave); tests/ha/test_entities.py (T2 creates, T6/T7 append)

## Preflight scan (plan self-review done while writing; defects fixed in commit 86d3c05)
| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T0↔T2↔T9 | __init__.py edits (prune/migrate/STATE consts; sweep prefixes; controller wiring) | sequential by wave; no conflict expected |
| T0/T2/T9 | en.json disjoint key groups | reconcile by scripted JSON union at merge |
| T1→T3 | read_float, to_celsius, temperature_unit_of, speed_convert | consistent |
| T2→T5/T6/T7 | HubView/CoverView fields, ControllerProtocol, signal_update, FakeController | consistent (controller builds every CoverView field) |
| T3→T5 | HubSignalSource/CoverSignalSet constructors and methods | consistent |
| T4→T5 | ScheduleTracker API | consistent |
| T5→T8 | EVENT_ACTION, snapshot(), started | consistent |
| T1 self | forecast test asserted an exact float | FIXED → approx |
| T3 self | hot-day test used signals() before seed(); room dwell test needed a raw-change update before the +11 min update | FIXED |
| T4 self | fire matching at exactly next_fire boundary (strict >) would miss the event; skipped-rule fixture didn't skip | FIXED → 30 s tolerance, re-arm from at; quiet 00:00–23:59 |
| T5 self | __import__ hack, _watched undeclared, frost_source expression, snapshot serialisation, double add_to_hass in a test | FIXED |
| T8 self | FakeController had no cover_views so the membership check skipped the reset | FIXED |
| engine parked | check_pending dam-while-disabled guard, prev_wind_active while disabled | not in plan (engine untouched); revisit at final review if async_set_enabled(False) with a pending record is reachable (it is) → Ruling below |
Ruling: engine parked items stay out of plan 2b's tasks; a small engine task is added only if the final review confirms user-visible impact — cost if wrong: a disabled-mid-flight cover may record an inert override that goes live on re-enable.
wave 1: BASE 86d3c055f78b3b5cb398a72412ed3b234ddf3ae1 — task-b0 (Task 0) ‖ task-b1 (Task 1) dispatched in parallel
wave 1: both implementers terminated by the session rate limit (429) mid-task; resumed after reset
Task 1: implementer DONE (8ea30d7; 235 passed) — review (sonnet) dispatched on review-86d3c05..8ea30d7.diff
Task 0: implementer DONE (d8f57d7; 232 passed) — review (sonnet) dispatched on review-86d3c05..d8f57d7.diff
Task 1: review (sonnet) — spec ✅, quality Approved, no Important findings. minor (deferred): naive-datetime fallback in forecast._entry_day untested; two separate imports from weather.const.
Task 1: complete (commits 86d3c05..8ea30d7, review clean); task/b1 merged into feature/ha-binding-b (fast-forward)
Task 0: review (sonnet) — spec ✅ ; 1 Important: store.prune(covers) prunes against the successfully-parsed covers, so a transiently misconfigured (broken_cover_config) subentry loses its persisted state every setup. minor (deferred): lost comment on the version guard in async_migrate_entry.
Ruling: prune against every cover subentry id ({s.subentry_id for s in entry.get_subentries_of_type(SUBENTRY_COVER)}) — "removed" means the subentry is gone, not that it failed to parse — cost if wrong: none.
Task 0: fix round 1/5 dispatched (resume implementer).
Task 0: fix round 1/5 done (e897a43; 233 passed) — scoped re-review (haiku) dispatched on d8f57d7..e897a43
Task 0: fix round 1/5 (1 addressed, 0 open — prune against all cover subentry ids; commits d8f57d7..e897a43)
Task 0: complete (commits 86d3c05..e897a43, review clean after fix round 1); task/b0 merged into feature/ha-binding-b (merge commit, disjoint files vs task/b1); merged branch: 244 passed, ruff + pyright clean
wave 2: BASE 60eb11aa212aca993a19ba7fa9d6fe4ef5217f80 — task-b2 (Task 2) ‖ task-b3 (Task 3) ‖ task-b4 (Task 4) dispatched in parallel; crossover: none expected (T2 touches __init__.py + en.json + new files; T3/T4 new files only)
Task 4: implementer DONE (2905b6d; 256 passed) — review (sonnet) dispatched on review-60eb11a..2905b6d.diff
Task 2: implementer DONE (4da18cd; 248 passed) — review (sonnet) dispatched on review-60eb11a..4da18cd.diff
Task 3: implementer DONE (59bf092; 260 passed) — brief's debounce test timestamps adjusted by +5 min (Debounce anchors the off-delay to the observed raw change, not to wall-clock now; engine untouched) — review (opus) dispatched on review-60eb11a..59bf092.diff
Task 4: review (sonnet) — spec ✅ ; 1 Critical: _fired never re-arms if on_fire raises (scheduler silently dead until midnight); 1 Important (plan-mandated): 30 s tolerance batching can fire a neighbouring profile early and the original profile twice when two profiles' rules fall within 30 s. minor (deferred): polar sun fallback untested; re-entrancy of _fired vs a concurrent async_arm.
Ruling (Critical): on_fire awaited inside try/except Exception with _LOGGER.exception; re-arm always runs (finally) — cost if wrong: none.
Ruling (Important): drop the tolerance matching; async_arm records the events it armed (all profiles whose next fire equals the earliest instant) and _fired fires exactly that recorded set, then re-arms from `at`; an arm generation counter makes a stale _fired skip its re-arm when a newer arm happened during on_fire — cost if wrong: none; strictly more precise.
Task 4: fix round 1/5 dispatched (resume implementer).
Task 2: review (sonnet) — spec ✅, Approved; 1 Important plan-mandated: entry_owned_issue_ids only knows rule_skipped_<profile_id>, while the plan's controller would create per-rule ids rule_skipped_<profile>_<index> that the stale sweep deletes on every reload. minor (deferred): SETUP_PREFIXES extra public tuple; missing_entity id format duplicated; hub_view property on the shared base.
Ruling: keep repairs.py as is; the controller (Task 5) raises ONE rule_skipped_<profile_id> issue per profile whose `rule` placeholder lists the skipped rule numbers ("1, 3") — ids stay profile-scoped and owned — cost if wrong: one combined issue instead of one per rule.
Task 2: complete (commits 60eb11a..4da18cd, review clean after ruling); task/b2 merged into feature/ha-binding-b
Task 3: review (opus) — spec ❌ 3 Important: (1) weather-availability tracking dead when a sunny override entity is configured; (2) frost_source_unavailable narrowed to "sensor configured" leaves weather-present-but-no-temperature unflagged; (3) HubSignalSource.next_check_at() ignores the weather/forecast grace expiries (no state event will ever wake the controller for them). Debounce test-timestamp adjustment judged correct. minor (deferred): wind_unit None disables conversion+mismatch; unit_of bare alias; room_unusable None branch untested; wind freeze-while-active untested; test hygiene (unused overrides/fixtures, 3-tuple); door read outside the snapshot.
Ruling (1): track weather availability before the override branch — cost if wrong: none. Ruling (2): frost_source_unavailable = `_outdoor_c() is None` unconditionally (the weather entity is a mandatory configured source); the controller decides how to report it — cost if wrong: an extra frost-source issue alongside the weather issue during long outages. Ruling (3): next_check_at includes weather-unavailable-since + grace and forecast-failed-since + grace — cost if wrong: none.
Task 3: fix round 1/5 dispatched (resume implementer).
Task 4: fix round 1/5 done (a5e5f43) — scoped re-review (sonnet) dispatched on 2905b6d..a5e5f43
Task 4: fix round 1/5 (2 addressed, 0 open; commits 2905b6d..a5e5f43) — re-review found 1 new Important in the fix diff: async_cancel does not bump the arm generation, so a cancel racing an in-flight _fired handler is undone by its finally-block re-arm (unload race).
Ruling: async_cancel bumps _generation (and clears _armed_at/_armed_covers) so a stale _fired skips its re-arm — cost if wrong: none.
Task 4: fix round 2/5 dispatched (resume implementer).
Task 3: fix round 1/5 done (a03e6ea; 263 passed) — scoped re-review (sonnet) dispatched on 59bf092..a03e6ea
Task 3: fix round 1/5 (3 addressed, 0 open — weather tracking before override, unconditional frost_source_unavailable, widened next_check_at; commits 59bf092..a03e6ea)
Task 3: complete (commits 60eb11a..a03e6ea, review clean after fix round 1); task/b3 merged into feature/ha-binding-b
Task 4: fix round 2/5 done (cd24082) — scoped re-review (haiku) dispatched on a5e5f43..cd24082
Task 4: fix round 2/5 (1 addressed, 0 open — cancel bumps generation; commits a5e5f43..cd24082)
Task 4: complete (commits 60eb11a..cd24082, review clean after 2 fix rounds); task/b4 merged into feature/ha-binding-b; merged wave-2 branch green (pytest, ruff, pyright)
wave 3: BASE 694e1b58329fe00f14a105ed34f285279ee3e45d — task-b5 (Task 5 controller, opus) ‖ task-b6 (Task 6 switch/select/button) ‖ task-b7 (Task 7 sensor/binary_sensor); crossover: tests/ha/test_entities.py appended by T6 and T7 (reconcile by concatenation), en.json untouched by all three
Carry to T5: rule_skipped is ONE issue per profile (rule_skipped_<profile_id>, placeholder rule = "1, 3"); HubSignalSource exposes frost_source_unavailable unconditionally (controller gates the weather-fallback case); HubSignalSource.next_check_at already includes grace expiries; hot_high_c/hot_low_c are private (_hot_high_c) in the adapter.
Task 6: implementer DONE_WITH_CONCERNS (69a9ef2; 276 passed) — added `reportIncompatibleVariableOverride = false` to [tool.pyright] because HA 2026.8's SwitchEntity.is_on / SelectEntity.current_option are cached_property/attribute-backed and @property overrides trip pyright.
Ruling: accept the pyright setting (HA's entity base classes are designed for @property overrides; the alternative of pushing state into _attr_* on every dispatch is more code for no behaviour gain); the same line may appear in task/b7 — identical hunks merge cleanly — cost if wrong: one class of pyright check disabled component-wide.
Task 6: review (sonnet) dispatched on review-694e1b5..69a9ef2.diff
Task 7: implementer DONE_WITH_CONCERNS (c04dde9; 275 passed) — used 15 scoped `# pyright: ignore[reportIncompatibleVariableOverride]` comments for the same @property-override issue Task 6 solved globally.
Ruling: keep Task 6's global setting; Task 9 removes Task 7's now-redundant scoped ignores (carry to T9) — cost if wrong: none.
Task 7: review (sonnet) dispatched on review-694e1b5..c04dde9.diff
Task 6: review (sonnet) — spec ✅, Approved, no Important findings; pyright ruling judged sound (verified against HA's cached_properties mechanism). minor (deferred): report miscount (nine vs eight classes); global rule disable rather than scoped.
Task 6: complete (commits 694e1b5..69a9ef2, review clean); task/b6 merged into feature/ha-binding-b
Task 5: implementer DONE (e8e4ccf; 282 passed) — deviations: view status recomputed post-act via engine.status(); asyncio.Lock serialises evaluations; cover_views is a read-only Mapping alias; concerns: midnight/forecast-refresh/set_cover_position fallback/snapshot/most setters untested here; setters before async_start save but skip evaluation. Review (opus) dispatched on review-694e1b5..e8e4ccf.diff
Task 7: review (sonnet) — spec ✅, Approved; minor (deferred): _iso duplicated in sensor.py/binary_sensor.py; report undercounts ignores (16). Task 7: complete (commits 694e1b5..c04dde9, review clean); task/b7 merged — tests/ha/test_entities.py conflict (both tasks appended at EOF) resolved by keeping both blocks in order, ruff-fixed imports; merged branch green
wave 3 merge: concatenation of the two appended test blocks cut the tail of Task 6's fake_with_cover (conflict hunk boundary inside the function); restored in f95e77a; full suite green (pytest, ruff, pyright)
Task 5: review (opus) — spec ❌ 5 Important: (1) _async_midnight/_async_forecast_tick/_async_weather_changed lack the `started` guard → a task racing async_stop can re-arm the schedule tracker after the cancel sweep (immortal timer); (2) `problem` flag never seeded at start; (3) the single evaluation lock is held across the blocking cover service call, stalling all covers and unload on one slow device; (4) write API mutates engine state outside the lock; (5) no controller test configures a schedule profile (arming, rule_fired, rule_skipped untested). Minor: (6) cover_unsupported false positive when the cover entity vanishes; (7) _on_issue_event unfiltered → re-entrant _publish mid-evaluation; (8) no immediate save on the command-failure path; (9) _sync_actual reads live state; (10) _publish clock; (11) last_engine_move in-memory only; (12) persistent_notification not in manifest dependencies (hassfest).
Ruling (1,2): add the started guards; seed _refresh_problem() in async_start — cost if wrong: none.
Ruling (3,4): per-cover asyncio.Lock (dict keyed by cover id) replaces the global lock; _act stays inside the per-cover critical section (only that cover waits on its device); async_stop clears started, cancels timers, then awaits each per-cover lock once; async_reset_override/async_set_enabled/async_set_mode take their cover's lock — cost if wrong: unload may wait for one in-flight service call per cover (bounded by HA's service timeout).
Ruling (5): add a controller test with a profile (fixed close rule) fired via time travel asserting rule_fired → close_cover, active_rule/next_event views, and a rule_skipped repair test — cost if wrong: none.
Ruling (6,7,8,12 upgraded to the fix round on the controller's judgement): gate _act on actual not UNAVAILABLE; filter issue events on domain == cover_automation; save immediately after on_command_sent even when the service call fails; add "persistent_notification" to manifest dependencies (hassfest requires imported integrations to be declared) — cost if wrong: none.
Task 5: minor (deferred): _sync_actual coalesces rapid transitions (comment it); _publish uses utcnow; last_engine_move not persisted.
Task 5: fix round 1/5 dispatched (resume implementer, opus).
wave 4 (early start): task-b8 (Task 8 services/logbook/diagnostics) branched from task/b5 @e8e4ccf so it can import controller.EVENT_ACTION while Task 5's fix round runs; disjoint files (services.py, services.yaml, logbook.py, diagnostics.py, tests) — merge after task/b5.
User direction (2026-09-16): this is a PRIVATE HACS custom repository like github.com/pujux/hass-aqara-h2-action-controller (hacs.json + hassfest/hacs workflows + README "Custom repositories" install + v0.x tags), not a public HACS listing.
Ruling (carry to T9): README install = HACS → Custom repositories → https://github.com/pujux/hass-cover-automation (category Integration) + manual copy alternative; manifest codeowners `@pujux`, documentation/issue_tracker URLs → github.com/pujux/hass-cover-automation; hacs.json keeps name/homeassistant/render_readme (drop nothing); CI keeps `ignore: brands` permanently (no brands PR); no repo topics/description work — cost if wrong: URLs to edit later.
Post-2b plan: merge → push to GitHub → tag v0.1.0 → add as HACS custom repository → shakedown in simulation mode.
Task 5: fix round 1/5 done (afdca05; 286 passed) — deviations: separate problem-seed test; schedule test needed a second open rule (a lone close rule makes yesterday's fire a standing hold) and a state-changing close handler. Concern surfaced: the engine gate has no "command already pending" suppression — two evaluations at the same instant (rule fire + fallback tick) both send when the cover has not started moving yet. Scoped re-review (sonnet) dispatched on e8e4ccf..afdca05.
Note for final review / possible engine follow-up: spec §1.3 gate 7 keys on actual == moving; a pending record with an unchanged actual lets a second evaluation re-send the same command (schedule/wind/door layers; shading is damped by gate 8). Consider suppressing while rt.pending targets the same state.
Task 8: implementer DONE_WITH_CONCERNS (02a5db8; 287 passed) — no "views" diagnostics key (followed the code sample/test); services test registers subentries before devices/entities (HA validates subentry existence); logbook constants imported from logbook.const/models for pyright. Review (sonnet) dispatched on review-e8e4ccf..02a5db8.diff.
Task 8: review (sonnet) — spec ✅, Approved; minor (deferred): SelectedEntities.log_missing() not called; silent no-op without a loaded controller; started-snapshot diagnostics branch untested until T9; services.yaml lacks name/description (translations come in T9); device-side resolution does not check the device belongs to our entry. Task 8: complete (commits e8e4ccf..02a5db8) — merge after task/b5.
Task 5: fix round 1/5 (all findings + 4 upgraded minors addressed; commits e8e4ccf..afdca05) — re-review found 1 new Important: no `started` re-check after acquiring a contended per-cover lock, so an evaluation queued before async_stop can run _act and re-arm a per-cover timer after the cancel sweep; plus the test's state-changing close handler hides the duplicate-send window (rule fire + fallback tick both send before the cover reports movement).
Ruling (round 2): re-check `started` immediately after acquiring each per-cover lock and make _arm_cover_timer a no-op when not started; AND the controller suppresses a Send whose target equals an existing pending record's target (command already in flight, any layer; debug log) — the engine gate keys on actual==moving only, which is a spec gap noted for the final review — cost if wrong: a retry of a lost command waits for the confirm window instead of the next evaluation (that is already the spec's design).
Task 5: fix round 2/5 dispatched (resume implementer).
Task 5: fix round 2/5 done (1a82bc6; 288 passed) — scoped re-review (sonnet) dispatched on afdca05..1a82bc6
Task 5: fix round 2/5 (2 addressed, 0 open — started re-check inside per-cover locks, in-flight command suppression; commits afdca05..1a82bc6). Out-of-scope note: _async_cover_timer checks started before, not after, acquiring the lock (narrow; check_pending never sends) — for the final review.
Task 5: complete (commits 694e1b5..1a82bc6, review clean after 2 fix rounds); task/b5 merged into feature/ha-binding-b
Task 8: complete; task/b8 merged after task/b5; merged branch green (pytest, ruff, pyright)
wave 5: Task 9 dispatched on feature/ha-binding-b directly (BASE 475b6b1e63375d34c419530ee4ed449490444301) — carries: private-HACS README/manifest URLs (@pujux), remove Task 7's redundant pyright ignore comments, runtime_data.controller wiring, services in async_setup, en.json entity+services translations.
Task 9: implementer DONE (6d25c62, 33f6124, 4f4d78f; 310 passed) — deviations: schedule scenario uses a two-rule profile (a lone close rule holds from yesterday's fire); test_controller.py sets CoreState.starting in 3 places (the entry now starts its own controller); unload saves the Store twice (harmless). Follow-up surfaced: engine check_pending `if p.enabled` guard is reachable end-to-end via async_set_enabled(False) with a pending record. Review (opus) dispatched on review-475b6b1..4f4d78f.diff
Task 9: review (opus) — spec ✅, Approved; 1 Important: async_at_started returns a no-op unsub when HA is already running (every reload), and controller.async_start awaits the forecast fetch BEFORE subscribing/setting started, so an unload during that await leaves an orphaned started controller driving covers. minor (deferred): double Store save on unload; manifest arrays reflowed; translation test uses subset not equality; literal entity id in an init test; CoreState.starting coupling in controller tests.
Ruling: async_stop sets a `_stopped` flag (independent of `started`); async_start re-checks it after every await and returns before subscribing/arming when set; the second Store save in async_unload_entry is dropped (async_stop already saves) — cost if wrong: none.
Task 9: fix round 1/5 dispatched (resume implementer).
Task 9: fix round 1/5 done (ca4244a; 311 passed) — scoped re-review (sonnet) dispatched on 4f4d78f..ca4244a; whole-branch package prepared review-86d3c05..ca4244a.diff
Task 9: fix round 1/5 (1 addressed, 0 open — _stopped flag, start re-checks, single unload save; commits 4f4d78f..ca4244a)
Task 9: complete (commits 475b6b1..ca4244a, review clean after fix round 1)
All 10 tasks complete. Branch feature/ha-binding-b: 26 commits over main 86d3c05, HEAD ca4244a, 311 tests, ruff + pyright clean.
Final whole-branch review (opus) dispatched on review-86d3c05..ca4244a.diff with this ledger (rulings, deferred minors, notes: engine gate in-flight gap; check_pending `if p.enabled` guard reachable via async_set_enabled(False) with a pending record; _async_cover_timer checks started before the lock; 2a final review's "Deferred to plan 2b" list for triage).
Final review (opus): Ready to merge WITH FIXES. 1 Critical: expired timer candidates (weather/forecast grace, stale retry time) + 1 s floor → endless 1 Hz evaluation loop during any >30 min weather outage. Important: (2) per-cover write API KeyError before start; (3) entities publish defaults until start; (4) reload while a position-only cover travels → spurious override (spec gap); (5) set_cover_position/unsupported paths untested; (6) no EVENT_CORE_CONFIG_UPDATE re-arm; (7) door/frost-conflict/quiet-hours/confirm-expiry/midnight untested through the controller. 12 minors + triage table (kept in the findings file / review output). 2a backlog: most absorbed; still open: entity-rename following, temperature_unit change path, re-evaluating missing_entity from state subscriptions, unused missing_entities field, unit-aware wind defaults.
Ruling (fix wave): one dispatch covering Critical 1, Important 2, 3, 5, 6, 7 and upgraded minors 8, 9, 11, 12; Important 4 deferred to the user (depends on whether the covers report opening/closing) with a spec decision; engine items (gate in-flight condition, check_pending enabled guard, prev_wind_active while disabled, persisted last_send_at) → engine follow-up task after merge; remaining minors → plan-3 backlog — cost if wrong: one more review round.
Ruling (ledger items weighed by the reviewer): (a) in-flight suppression stays in the controller for now, moves into the engine gate later; (b) check_pending's decision-27 branch lacks the enabled guard — real, low probability, engine follow-up; (c) _async_cover_timer started check moves inside the lock (F8).
Final fix wave dispatched (opus) with final-fix-wave-findings.md; FIX_BASE ca4244a.
Final fix wave DONE (c70a405, 069347e, d67486c, 64f2bab, 7057f94; 323 passed ×3) — extra controller fault found and fixed mid-wave: _arm_cover_timer used the pre-act StepResult so a fresh command's confirm deadline was invisible (now re-reads classify.pending_deadline after acting). Scoped re-review (opus) dispatched on ca4244a..7057f94
Final fix wave re-review (opus): F1–F12 all ADDRESSED, no new breakage; the twelve new tests re-run green. Out-of-scope observations → plan-3 backlog: ScheduleTracker._fired runs as a plain hass task (make it entry-owned); the no-1 Hz-loop guarantee relies on the engine invariant that only the pending deadline in StepResult.next_check_at can be in the past (add a debug log/assert on sub-second arms); async_block_till_done does not await background tasks (note for future tests needing wait_background_tasks=True); frost notification dismiss runs on every non-frost evaluation (cheap); controller construction setdefaults empty CoverPersisted records before start.
Final review clean after the fix wave. Branch head 7057f94, 323 tests. Plan 2b execution complete.
